- title "Cops"
= form_tag cops_path, :method => 'get', :id => 'cops_search' do
  #cops
    = render :partial =>'cops', :locals => {:cops => @cops}
- if user_signed_in?
  %p= link_to "New Cop", new_cop_path
