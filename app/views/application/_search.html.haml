= form_tag cops_path, :method => :get, :id => "cops_search", :class => "form-search navbar-search" do
  .input-append
    = search_field_tag :search, params[:search], :id => 'search_field', :class => 'span2 search-query', :placeholder => "Search"
    = button_tag :type => "submit", :name => nil, :class=> 'btn' do
      %i.icon-search
